# Porta do Nuxt Web App
PORT=3001

# URL interna do Backend Katari (server-consorcio) — apenas lida no servidor Nuxt (BFF)
# Esta variável NUNCA é exposta ao navegador.
NUXT_BACKEND_BASE=http://localhost:3000
