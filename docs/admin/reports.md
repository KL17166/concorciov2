# Admin — Reports (relatórios)

Fontes: `server-consorcio/src/routes/admin/reportRoutes.ts` (1 rota: 1× `router.get`, linha 7),
`server-consorcio/src/controllers/admin/reportsController.ts` (`getReports`, linhas 6–336),
`server-consorcio/src/views/pages/reports/index.ejs`.
Prefixo: `/admin`. Gate `isAdmin` + `requireCapability('reports.view')`.

## Rotas (1/1 — conferida contra `reportRoutes.ts:7`)

### `GET /admin/reports`
- **Gate:** `isAdmin` + `requireCapability('reports.view')` (`reportRoutes.ts:7`)
- **Ativado por (tela):** `pages/reports/index.ejs` — sidebar (`path:'/reports'`); barra de período: links `GET /admin/reports?period=today|week|month|3months|year` (`index.ejs:33-37`, ativo via `period`); form `method="GET" action="/admin/reports"` com `start/end` (date inputs `:40-42`) + `hidden period=custom` + submit filtro (`:31-48`); botão "Exportar CSV" (`:13-15`) é **só client-side** (`exportReport()` lê `#transactionTable` e baixa CSV, `:568-581`) — não dispara rota
- **Request:** query `period?: 'today'|'week'|'month'|'3months'|'year'|'custom'` (default `'month'`, controller `:11`); `start?: string(date)` + `end?: string(date)` (só usados se `period=custom`, `:12-13,:38-41`). Semântica (`:19-44`): `today` = hoje 00:00→agora; `week` = −6 dias 00:00→agora; `month` = dia 1 00:00→agora; `3months` = dia 1 de (mês−2)→agora; `year` = 01/01→agora; `custom` = `start` (ou dia 1) → `end 23:59:59` (ou agora); default/unknown = mês atual. `periodEnd` default = hoje 23:59:59 (`:16-17`). Período anterior de mesma duração p/ Δ%: `prevEnd = periodStart−1ms`, `prevStart = prevEnd−duração` (`:47-49`)
- **Resposta do servidor:** `render('pages/reports/index', {...})` (`:275-331`) com vars: `path:'/reports'`; `period:string`, `periodStart:Date`, `periodEnd:Date`, `customStart:string`, `customEnd:string`; `stats:{ totalClients, totalContracts, activeContracts, contemplatedContracts, totalReceived, totalOverdue, totalPending, totalRefunded, totalProducts, totalBids, pendingBids, paidCount, overdueCount }` (`:286-300`); `period_stats:{ periodReceived, prevPeriodReceived, receivedChange:number|null, avgTicket, defaultRate, paidCount, overdueCount }` (`:303-311`, `receivedChange=null` sem base `:92-94`); `projections:{ proj30, proj60, proj90 }` (`:314` — soma de PENDING com `dueDate` em +30/+60/+90d, `:113-121`); `monthlyRevenue:[{month,amount}]×12` (`:124-135`, por `paymentDate`); `statusDistribution:[{label,value,color}]` (Pago/Pendente/Atrasado/Reembolsado, `:148-153`); `paymentMethods:[{method,total}]` (por `paymentMethod||'N/A'` no período, `:138-145`); `revenueByType:[{type,total}]` (por `product.type`, `:233-240`); `newClientsPerMonth:[{month,count}]×6` (role CLIENT, `:214-230`); `transactionFeed:[{id,clientName,clientEmail,productName,amount,paymentMethod,paymentDate,installment}]` (50 pagas recentes do período, `:156-168`); `refundFeed:[{id,clientName,productName,amount,dueDate}]` (20 reembolsos, `:171-180`); `topClients:[{name,email,total,count}]` (top 10 por valor pago no período, `:183-194`); `overdueClients:[{name,total,count}]` (OVERDUE com `dueDate ≤ −30d`, top 10, `:197-211`); `topProducts:[{name,count,revenue}]` (top 5 por receita no período, `:243-254`)
- **Efeitos:** somente leitura — `installment.findMany` (1 query com `subscription→user/plan→product`, `:52-69`, agrupado em JS por status/data `:71-74`); `user.findMany` CLIENT 6m (`:214-220`); `Promise.all` de 7 counts (`:257-273`: users CLIENT, subscriptions total/ativas/contempladas, produtos ativos, bids total/pendentes). Nenhum write, e-mail ou alerta
- **Erros:** `catch` → `res.status(500).send('Erro ao carregar relatórios')` (`:332-335`, log `:333`); `period` inválido cai no `default` (mês atual, `:42-43`) sem erro; base vazia → zeros/`receivedChange=null`/`avgTicket=0` (`:92-102`)
